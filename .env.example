# Simple LLM Backend 환경설정 파일
# 이 파일을 .env로 복사하여 사용하세요

# ========================================
# LLM 엔진 설정
# ========================================

# vLLM 설정
VLLM_ENABLED=true
VLLM_BASE_URL=http://localhost:8000

# SGLang 설정
SGLANG_ENABLED=true
SGLANG_BASE_URL=http://localhost:30000

# ========================================
# 데이터베이스 설정 (선택사항)
# ========================================

# Oracle 데이터베이스
DB_HOST=localhost
DB_PORT=1521
DB_SID=XE
DB_USERNAME=llmchat
DB_PASSWORD=your_password

# ========================================
# Spring 설정
# ========================================

# 실행 프로필 (dev, prod 중 선택)
SPRING_PROFILES_ACTIVE=dev

# 서버 포트 (기본값: 8080)
SERVER_PORT=8080

# ========================================
# 로깅 설정
# ========================================

# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# 로그 파일 경로
LOG_FILE_PATH=logs/simple-llm-backend.log

# ========================================
# 보안 설정 (운영환경용)
# ========================================

# JWT 비밀키 (운영환경에서는 반드시 변경)
JWT_SECRET=your-secret-key-change-this-in-production

# ========================================
# 기타 설정
# ========================================

# 타임아웃 설정 (초)
REQUEST_TIMEOUT=30

# 최대 토큰 수
MAX_TOKENS=512

# Temperature 설정
TEMPERATURE=0.7
# Simple LLM Backend 환경설정 파일 예시
# 이 파일을 .env 로 복사하여 실제 환경에 맞게 수정하세요

# ===========================================
# LLM 엔진 설정
# ===========================================

# vLLM 설정
VLLM_ENABLED=true
VLLM_BASE_URL=http://localhost:8000
VLLM_TIMEOUT=30
VLLM_MAX_TOKENS=512
VLLM_TEMPERATURE=0.7

# SGLang 설정
SGLANG_ENABLED=true
SGLANG_BASE_URL=http://localhost:30000
SGLANG_TIMEOUT=30
SGLANG_MAX_TOKENS=512
SGLANG_TEMPERATURE=0.7

# LLM 페일오버 설정
LLM_FAILOVER_ENABLED=false
LLM_RETRY_ATTEMPTS=3
LLM_RETRY_DELAY=1s

# ===========================================
# 데이터베이스 설정
# ===========================================

# Oracle 데이터베이스 (운영환경용)
DB_HOST=localhost
DB_PORT=1521
DB_SID=XE
DB_USERNAME=llmchat
DB_PASSWORD=your_password
DB_DRIVER=oracle.jdbc.OracleDriver

# H2 데이터베이스 (개발환경용) - 기본값
# DATABASE_URL=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=Oracle
# DB_USERNAME=sa
# DB_PASSWORD=

# ===========================================
# 서버 설정
# ===========================================

# 서버 포트
SERVER_PORT=8080

# Spring 프로필 (dev, prod, test)
SPRING_PROFILES_ACTIVE=dev

# ===========================================
# JVM 및 성능 설정
# ===========================================

# JVM 옵션 (Windows 최적화)
JAVA_OPTS=-Xms512m -Xmx2g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8 -Djava.awt.headless=true

# ===========================================
# 로깅 설정
# ===========================================

# 로그 레벨
LOG_LEVEL=INFO
LOG_FILE_PATH=logs/simple-llm-backend.log

# ===========================================
# 보안 설정
# ===========================================

# JWT 시크릿 키 (운영환경에서는 반드시 변경)
JWT_SECRET=your-secret-key-change-this-in-production

# ===========================================
# Redis 설정 (선택사항)
# ===========================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ===========================================
# 모니터링 설정
# ===========================================

# Actuator 엔드포인트 노출 설정
ACTUATOR_ENDPOINTS=health,info,metrics

# H2 웹 콘솔 활성화 (개발환경만)
H2_CONSOLE_ENABLED=true

# ===========================================
# 기능 플래그
# ===========================================

# Mock 응답 활성화 (개발/테스트용)
ENABLE_MOCK=false

# 상세 로깅 활성화
ENABLE_DETAILED_LOGGING=false